# Receipt Ledger

Only reviewed `efficiency-<12-hex>.json` public exports belong here. Do not add prompts, responses, paths, repository or user identifiers, request ids, prices, or raw host logs.

The receipt self-hash detects changed content; it does not authenticate the submitter or provider.
